# Cryptocurrency Price Tracker — requirements
# Install: pip install -r requirements.txt

# Core (required)
requests>=2.31.0
pandas>=2.0.0

# Only needed if USE_SELENIUM = True in scraper.py
selenium>=4.18.0
webdriver-manager>=4.0.1
